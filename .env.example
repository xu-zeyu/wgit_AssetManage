# 前端请求基地址：
# 1. 代理模式（推荐用于本地开发 / 测试环境）：填 /api
# 2. 直连模式（如生产网关统一转发）：填完整地址，例如 https://api.example.com
NEXT_PUBLIC_API_BASE_URL=/api

# 仅在 NEXT_PUBLIC_API_BASE_URL=/api 时生效，由 Next.js rewrites 代理到后端服务
AUTH_PROXY_TARGET=https://auth-api.wgit123.com
API_PROXY_TARGET=https://ams-api.wgit123.com
